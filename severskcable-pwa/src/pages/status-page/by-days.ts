// for (let i = dayjs(getPeriodDayjs(period)[0]).date(); i <= dayjs(getPeriodDayjs(period)[1]).date(); i++) {
//     const factCount = (Math.round(hoursByDayInPeriod[factPercCount].breakHours*planCount/(24)) - planCount) * (-1);
//     chartData.push({
//         name: i,
//         План: planCount,
//         Факт: factCount
//     })
//     planCount = planCount + Math.round(100 / (dayjs(getPeriodDayjs(period)[1]).date()));
//     factPercCount++;
// }
export const i =1;